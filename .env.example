# Nubla SIEM — .env.example (plantilla)
# Copia este archivo a ".env" y rellena/ajusta los valores.
# NO subas el archivo .env real al repositorio.

# ---- General ----
LOG_LEVEL=INFO
TENANT_ID=default  # Se usará mientras no activemos multitenencia estricta

# ---- RabbitMQ (AMQP) ----
RABBITMQ_HOST=rabbitmq
RABBITMQ_USER=admin
RABBITMQ_PASSWORD=securepass
# Debe coincidir con los defaults de docker-compose y Fluentd
RABBITMQ_EXCHANGE=logs_default
RABBITMQ_QUEUE=nubla_logs_default
RABBITMQ_ROUTING_KEY=nubla.log.default

# ---- Elasticsearch ----
# Por defecto, el compose usa "elasticsearch" (host del servicio)
# Alternativamente: http://elasticsearch:9200
ELASTICSEARCH_HOST=elasticsearch