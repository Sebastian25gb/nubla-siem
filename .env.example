# Nubla SIEM – Ejemplo de variables (no usar en producción)
# Copiar a .env y ajustar según modo de ejecución.

# -------------------------
# Modo HOST (scripts/venv)
# Descomentar estas si ejecutas backend y consumer directamente en tu máquina:
# RABBITMQ_HOST=127.0.0.1
# ELASTICSEARCH_HOST=127.0.0.1:9201

# Modo DOCKER (contenedores en misma red)
RABBITMQ_HOST=rabbitmq
ELASTICSEARCH_HOST=opensearch:9200

# -------------------------
RABBITMQ_USER=admin
RABBITMQ_PASSWORD=securepass
RABBITMQ_EXCHANGE=logs_default
RABBITMQ_QUEUE=nubla_logs_default
RABBITMQ_ROUTING_KEY=nubla.log.default
RABBITMQ_DLX=logs_default.dlx
RABBITMQ_DLQ=nubla_logs_default.dlq

TENANT_ID=default
LOG_LEVEL=INFO

# Ruta al schema local (en host puede necesitar prefijo absoluto)
NCS_SCHEMA_LOCAL_PATH=backend/app/schema/ncs_v1.0.0.json

# Flags de migración
FEATURE_USE_OPENSEARCH=true
USE_MANUAL_DLX=false  # true => publish to DLX with reason header instead of basic_nack

# Seguridad OpenSearch (descomentar si está habilitada)
# OS_USER=admin
# OS_PASS=admin

# NO SUBIR SECRETOS reales a git.