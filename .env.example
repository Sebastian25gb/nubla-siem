# Nubla SIEM — .env.example (plantilla transicional)
# Copia este archivo a ".env" y rellena/ajusta los valores.
# NO subas tu .env al repositorio.

# ---- General ----
ENV=local
LOG_LEVEL=INFO
TENANT_ID=default

# ---- RabbitMQ (LEGACY) ----
RABBITMQ_HOST=rabbitmq
RABBITMQ_USER=admin
RABBITMQ_PASSWORD=securepass
RABBITMQ_EXCHANGE=logs_default
RABBITMQ_QUEUE=nubla_logs_default
RABBITMQ_ROUTING_KEY=nubla.log.default

# ---- Elasticsearch / OpenSearch (transición)
# ELASTICSEARCH_HOST se usa por compatibilidad con el backend actual.
# Para la transición, apunta a opensearch: http://opensearch:9200
ELASTICSEARCH_HOST=http://opensearch:9200

# ---- OpenSearch (opcional) ----
OPENSEARCH_URL=http://opensearch:9200
OPENSEARCH_USERNAME=admin
OPENSEARCH_PASSWORD=admin

# ---- Flags de migración (local/dev) ----
FEATURE_USE_OPENSEARCH=true